bundle:
  name: browser-runtime
  version: 0.1.0
  description: |
    Browser runtime behavior with browser-specific tools.
    BROWSER-ONLY: These tools require Pyodide JS bridge.

includes:
  - bundle: browser:behaviors/browser-context

tools:
  - module: tool-browser-storage
    source: browser:modules/tool-browser-storage
