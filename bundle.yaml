bundle:
  name: browser
  version: 0.1.0
  description: |
    Browser development and runtime bundle for Amplifier.
    
    Default: CLI-safe context and agents for building browser apps.
    For browser runtime: use behaviors/webruntime-core explicitly.

includes:
  - bundle: browser:behaviors/webruntime-agents
